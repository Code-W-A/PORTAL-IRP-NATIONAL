/** Soft cap for combined bulk print PDF (memory / serverless timeout). */
export const BULK_PDF_MAX_IDS = 40;

/** Parallel PDF fetches when building a ZIP archive. */
export const BULK_ZIP_CONCURRENCY = 3;
